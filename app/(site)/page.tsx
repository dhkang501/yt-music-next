import { sleep } from '@/lib/utils'
import { error } from 'console'
import React from 'react'

const home = async () => {
  // await sleep(2000);
  // throw new Error("my error");
  return (
    <div>HomePage</div>
  )
}

export default home